{
  "//": "Every file in this folder is written as an ES module (import/export, import.meta.url), so this must say \"module\" — Node reads the NEAREST package.json, not the root one, and a \"commonjs\" here makes it reject the very first import line with 'Cannot use import statement outside a module'. Dependencies still come from the root node_modules; there is nothing to install in here.",
  "name": "dubai-fine-clean-server",
  "private": true,
  "type": "module"
}
